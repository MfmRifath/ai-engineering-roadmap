---
book: "Build a Large Language Model (From Scratch)"
book_slug: "03-build-llm-from-scratch-raschka"
chapter: 7
title: "Fine-tuning to Follow Instructions"
status: notes-written
difficulty: 4
est_hours: 6
tags: [sft, instruction-tuning, masking, lora, evaluation, critical]
---

# R7 — Fine-tuning to Follow Instructions

> **Thesis:** The difference between a text completer and an assistant is one supervised
> fine-tuning run on (instruction, response) pairs — and the details of formatting and loss
> masking matter more than the training itself.

## Why this chapter matters

The final chapter, and it closes the loop: you built a model, pretrained it, and now you
**post-train** it into something that follows instructions. This is exactly the SFT stage every
commercial assistant goes through, at a scale you can run yourself.

It also delivers the payoff on the base/instruct distinction from
[A1](../../02-hands-on-llms-alammar/notes/ch01.md) — you *perform* the conversion rather than
reading about it.

## Core concepts

### The dataset

1,100 (instruction, optional input, output) triples. Small, and that is the point: **instruction
tuning needs far less data than pretraining** because it teaches *format and behaviour*, not
knowledge. The LIMA paper made this case with 1,000 examples; Raschka's dataset is the same order.

Split 85/5/10 train/val/test.

### Prompt formatting

Raschka uses the Alpaca style:

```
Below is an instruction that describes a task. Write a response that
appropriately completes the request.

### Instruction:
{instruction}

### Input:
{input}          ← omitted entirely when there is no input

### Response:
{output}
```

**Consistency is everything.** The model learns "after `### Response:` comes an answer." Vary the
format across examples and you teach ambiguity. Vary it between training and inference and the
model is off-distribution.

`[dated]` Modern models use **chat templates** with role markers (`<|im_start|>user`, etc.)
rather than Alpaca-style prose headers. Same principle — a consistent structural signal — better
executed. `tokenizer.apply_chat_template()` is the modern equivalent, and using the wrong one is
the top cause of fine-tunes that appear to do nothing
([A12](../../02-hands-on-llms-alammar/notes/ch12.md)).

Raschka also compares against the **Phi-3 template**, which is much terser — a good demonstration
that the specific format matters less than using the model's own.

### Batching with a custom collate function

The most technically interesting part of the chapter. The custom collator does four things:

1. **Pad to the longest sequence in the batch** — not to the dataset maximum. Per-batch padding
   saves substantial compute.
2. **Build targets as inputs shifted by one**, as in [R2](ch02.md).
3. **Replace padding tokens in the targets with `-100`.**
4. **Optionally mask the instruction portion too.**

That `-100` is the detail worth internalizing. PyTorch's `cross_entropy` has
`ignore_index=-100` by default, so any target position set to −100 **contributes nothing to the
loss and no gradient**.

Why it matters: without it, the model spends capacity learning to predict padding tokens, which
is both useless and actively harmful — pad tokens are frequent, so the model learns to emit them.

**Instruction masking.** You can also set the instruction tokens to −100 so the loss applies only
to the response. Raschka discusses both and notes the research is mixed — masking the instruction
is standard practice (it is what "completion-only training" means), though some results suggest
training on the full sequence helps slightly. Worth knowing the debate exists.

### Training

Identical loop to [R5](ch05.md). Two epochs at lr 5e-5 with AdamW; a few minutes on a GPU, longer
on CPU.

Raschka uses **GPT-2 medium (355M)** rather than small here, because 124M produces
instruction-following that is too weak to evaluate meaningfully. A practical and honest choice.

### Evaluation — the hard part

How do you score an open-ended response? There is no exact match.

Raschka's approach: **generate responses on the test set, then use a stronger model (Llama 3 8B
via Ollama) to score each on 0–100** against the reference.

This is **LLM-as-a-judge** ([H3](../../04-ai-engineering-huyen/notes/ch03.md)), and encountering
it here — as the only workable option, arrived at by necessity — is the right introduction. You
feel the problem before Huyen systematizes it.

The caveats Raschka notes, which Huyen expands:

- Judges have **position bias**, **verbosity bias** (longer answers score higher), and
  **self-preference** (models favour their own outputs).
- Absolute scores are unreliable; **relative comparisons** are more trustworthy.
- **You must validate the judge** against human ratings on a sample, or you are optimizing an
  unexamined proxy.

### Appendix E — LoRA

The natural follow-on: everything in this chapter, but training ~0.1% of the parameters.

Raschka implements LoRA from scratch — a `LoRALayer` and a `LinearWithLoRA` wrapper that replaces
`nn.Linear` modules. Around 30 lines. Doing it by hand makes
[A12](../../02-hands-on-llms-alammar/notes/ch12.md) and
[H7](../../04-ai-engineering-huyen/notes/ch07.md) far more concrete than any diagram.

**Do Appendix E.** It is the single highest-value optional section in the book.

## Key code

```python
def custom_collate_fn(batch, pad_token_id=50256, ignore_index=-100,
                      allowed_max_length=None, device="cpu"):
    batch_max_length = max(len(item) + 1 for item in batch)   # pad per BATCH, not dataset
    inputs_lst, targets_lst = [], []

    for item in batch:
        new_item = item + [pad_token_id]
        padded = new_item + [pad_token_id] * (batch_max_length - len(new_item))

        inputs = torch.tensor(padded[:-1])
        targets = torch.tensor(padded[1:])                     # shifted by one

        # -100 => ignored by cross_entropy: no loss, no gradient.
        # Keep the FIRST pad token as a real target so the model learns to stop.
        mask = targets == pad_token_id
        indices = torch.nonzero(mask).squeeze()
        if indices.numel() > 1:
            targets[indices[1:]] = ignore_index

        if allowed_max_length is not None:
            inputs, targets = inputs[:allowed_max_length], targets[:allowed_max_length]

        inputs_lst.append(inputs)
        targets_lst.append(targets)

    return (torch.stack(inputs_lst).to(device),
            torch.stack(targets_lst).to(device))
```

Note the subtlety in that mask: the **first** padding token is left as a real target, so the model
learns where a response *ends*. Mask all of them and it never learns to stop.

## Gotchas

- **Inconsistent prompt formatting.** Training and inference must use the identical template.
- **Not masking padding in the targets.** The model learns to emit pad tokens.
- **Masking *all* padding including the first.** The model never learns to terminate and generates
  until it hits the token limit.
- **Padding to the dataset maximum.** Wastes compute on every batch; pad per batch.
- **Forgetting `ignore_index=-100` is the default.** If you pick a different sentinel you must
  pass it to `cross_entropy` explicitly.
- **Using GPT-2 small for instruction tuning.** Too weak to evaluate; use medium or larger.
- **Trusting LLM-judge scores without validation.** Check against human ratings on a sample.
- **Expecting a fine-tune to add knowledge.** It teaches format and behaviour. Facts come from
  RAG ([H7](../../04-ai-engineering-huyen/notes/ch07.md)).

## Exercises

**Understand**
1. Why does instruction tuning need only ~1,000 examples when pretraining needs trillions of
   tokens?
2. What does `-100` do in the targets, and why does it matter?
3. Why keep the first padding token unmasked?
4. Name three biases of LLM-as-a-judge evaluation.

**Build**
1. Fine-tune GPT-2 medium on the instruction dataset and generate responses to held-out prompts.
   **Compare against the base model's answers to the same prompts** — that side-by-side is the
   whole chapter.
2. Exercise 7.1 — retrain using the **Phi-3 prompt template** instead of Alpaca and compare.
3. Exercise 7.2 — mask the instruction tokens in the loss and compare against training on the
   full sequence. Form your own view on the open question.
4. Exercise 7.3 — fine-tune on the original Alpaca 52k dataset. Note what more data buys.
5. **Do Appendix E**: implement LoRA from scratch, apply it to this fine-tune, and compare final
   quality against full fine-tuning while tracking trainable parameter count and peak memory.
6. Build a small LLM-as-a-judge harness. Then hand-rate 20 of the same responses yourself and
   measure the correlation. **This is your first evaluation validation**, and it sets up
   [p4](../../../projects/p4-llm-eval-harness/).

## Flashcards

<!-- card -->
Q: Why does instruction tuning need so much less data than pretraining?
A: It teaches format and behaviour — "text after this marker is a request, respond to it" — rather than knowledge about the world. The knowledge is already in the pretrained weights. Roughly a thousand well-curated examples can suffice.
<!-- /card -->

<!-- card -->
Q: What does setting a target token to `-100` do?
A: PyTorch's `cross_entropy` uses `ignore_index=-100` by default, so those positions contribute no loss and no gradient. It is how padding (and optionally the instruction portion) is excluded from training.
<!-- /card -->

<!-- card -->
Q: Why keep the *first* padding token unmasked in the targets?
A: It is the end-of-sequence signal. If every pad token is masked, the model never learns where a response should stop and generates until it hits the token limit.
<!-- /card -->

<!-- card -->
Q: Why pad to the longest sequence in the *batch* rather than the dataset?
A: Per-batch padding avoids computing over pad tokens that no example in that batch needs, which is a substantial compute saving when sequence lengths vary widely.
<!-- /card -->

<!-- card -->
Q: What is LLM-as-a-judge, and what are its main biases?
A: Using a stronger model to score another model's open-ended outputs. Main biases: position bias (order of presentation), verbosity bias (longer answers score higher), and self-preference (models favour their own style). Relative comparisons are more reliable than absolute scores, and the judge must be validated against human ratings.
<!-- /card -->

<!-- card -->
Q: What are the two stages of post-training, and which does this chapter cover?
A: Supervised fine-tuning on (instruction, response) pairs — this chapter — followed by preference tuning (RLHF or DPO), which teaches which of two valid responses humans prefer.
<!-- /card -->

## Cross-links

| Book | Where | Angle |
|---|---|---|
| Géron | [G11](../../01-hands-on-ml-geron/notes/ch11.md) | Transfer learning |
| Alammar | [A1](../../02-hands-on-llms-alammar/notes/ch01.md) | The base/instruct distinction, now performed |
| Alammar | [A12](../../02-hands-on-llms-alammar/notes/ch12.md) | LoRA, QLoRA, DPO — the modern toolchain |
| Huyen | [H3](../../04-ai-engineering-huyen/notes/ch03.md) | LLM-as-a-judge, rigorously |
| Huyen | [H7](../../04-ai-engineering-huyen/notes/ch07.md) | Whether to fine-tune |
| Huyen | [H8](../../04-ai-engineering-huyen/notes/ch08.md) | Building the instruction dataset |

[Concept matrix](../../../CURRICULUM.md) · [Prev: ch06](ch06.md) · **End of Raschka** →
[Huyen ch01](../../04-ai-engineering-huyen/notes/ch01.md)
